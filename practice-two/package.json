{
  "name": "practice-two",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel index.html",
    "build": "rm -rf build && rm -rf .parcel-cache && parcel build ./*.html --no-cache --no-source-maps --dist-dir dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.9.3"
  }
}
